# Evolución determinista sin mutación
populations:
  dominant:
    n: 60
    fitness: 2.0
  weak:
    n: 40
    fitness: 1.0

steps: 200

engine:
  type: deterministic

output: results_deterministic.xlsx
